import type { Campaign } from '../../../../engine/types';
import { HardIcon } from '../../icons';
import { hardTheme } from './theme';

export const hardCampaign: Campaign = {
  id: 'hard',
  name: 'СЛОЖНО',
  label: 'Эксперт',
  icon: HardIcon,
  theme: hardTheme,
};

