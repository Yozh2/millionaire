import type { Campaign } from '../../../../engine/types';
import { EasyIcon } from '../../icons';
import { easyTheme } from './theme';

export const easyCampaign: Campaign = {
  id: 'easy',
  name: 'ЛЕГКО',
  label: 'Новичок',
  icon: EasyIcon,
  theme: easyTheme,
};

