export { getBasePath, withBasePath } from '@engine/assets/paths';
export {
  publicFile,
  publicDir,
  gameDir,
  gameFile,
  gameIconsDir,
  gameIconsFile,
  gameImagesDir,
  gameImagesFile,
  type GamePublicDir,
} from '@engine/assets/public';

