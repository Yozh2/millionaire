export { getBasePath, withBasePath } from '@engine/assets/paths';
export {
  publicFile,
  publicDir,
  gameDir,
  gameFile,
  gameIconsDir,
  gameIconsFile,
  gameFaviconDir,
  gameFaviconFile,
  gameImagesDir,
  gameImagesFile,
  type GamePublicDir,
} from '@engine/assets/public';
