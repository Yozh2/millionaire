# План улучшений движка

Статусы: 🔴 todo · 🟡 in-progress · 🟢 done

## Задачи
- 🟢 Подсказки: выровнять контент по центру (иконка + текст с отступом), уменьшить минимальную ширину, жёстко задать 3 лейаута (4 в ряд / 2×2 / 1×4) с растяжением по ширине; сохранить быстрый обратный блик на нажатие.
- 🟢 Prize ladder: уменьшить ширину колонки номера; центрировать колонку стоимости, но с фиксированным блоком ширины для правого выравнивания цифр; колонка сложности — фикс. ширина и левое выравнивание звёзд.
- 🟢 Прогнать проверки (lint/тесты при наличии) после изменений и зафиксировать статус.
- 🟢 Исправить: перекрытие звёзд стоимости в prize ladder и сделать подъём подсказок при ховере/нажатии независимым от размера (фикс. величина).

## Журнал
- 2025-12-09: Создан план, добавлена headless-вариация панелей, убраны header'ы в шапках и вопросе, обновлены строки/конфиги, очищен лишний текст на экране выбора кампании.
- 2025-12-09: Прогнан `npm run lint` (остались прежние предупреждения по react-hooks/exhaustive-deps и лишней eslint-disable в useFavicon).
- 2025-12-09: Вернул header в панели вопроса (номер #NN слева, цена справа), убрал звёзды сложности из панели, сделал кнопки подсказок фиксированного размера, подправил строки `questionHeader` под новый формат, повторно прогнал `npm run lint` (предупреждения прежние).
- 2025-12-09: Добавил неразрывный пробел в номере вопроса, убрал header панели подсказок и сузил кнопки на десктопе, добавил отступ до подсказок, выровнял prize ladder вправо и добавил бегущий блик на текущей строке.
- 2025-12-09: Прогнал `npm run lint` после правок (предупреждения прежние: useAudio deps, лишний eslint-disable в useFavicon).
- 2025-12-09: Подсказки — ускоренный обратный блик, авто-fit раскладка с фиксированным контент-блоком и иконкой/лейблом слева; prize ladder — узкая колонка номера, центрированная стоимость, фиксированная ширина звёзд слева; повторно прогнан `npm run lint` (предупреждения прежние).
- 2025-12-09: Исправлен оверлап звёзд с ценой (расширены боковые поля/звёзды), подъём подсказок теперь фиксированный по пикселям.
- 2025-12-09: Расширил min-width prize ladder (боковые поля/значение) и добавил nowrap для стоимости; `npm run lint` (предупреждения прежние).
- 2025-12-09: Перестроил prize ladder на сетку с фикс. колонками и растяжимыми пустыми, центрировал стоимость без переносов; унифицировал questionHeader в конфигах, цвет цены = цвет номера, убрал кавычки у реплик компаньонов.
- 2025-12-09: Прогнал `npm run lint` (предупреждения прежние: useAudio deps, eslint-disable в useFavicon).
- 2025-12-09: Привёл prize ladder к колонкам A-B-C-D-E, расширил фикс. ширины для стоимости, убрал эмодзи с action/new game и end headers, унифицировал блик (единые keyframes).
- 2025-12-09: Подсказки центрированы (иконка+текст), уменьшена минимальная ширина, лейауты 4/2/1 с растяжением; prizeladder — уменьшена колонка номера, стоимость центрирована с правым выравниванием внутри фиксированного блока, звёзды фиксированной ширины слева; `npm run lint` (предупреждения прежние).
