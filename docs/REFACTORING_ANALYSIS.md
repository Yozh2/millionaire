# 🔍 Анализ кодовой базы: Millionaire Quiz Engine

**Дата анализа:** 7 декабря 2025 г.
**Обновление:** Статусы обновлены после фаз 1–4 (legacy удалён, ESLint/Prettier, ErrorBoundary, logger, CI).

---

## 1. 🗑️ УСТАРЕВШИЙ / ДУБЛИРУЮЩИЙСЯ КОД

### 1.1 `src/utils/soundEffects.ts` — **ПОЛНОСТЬЮ УСТАРЕВШИЙ**

Это **старый модуль звуков** (~456 строк), который полностью дублирует функциональность `src/engine/utils/audioPlayer.ts`:
- Собственный `AudioContext`, `audioCache`, `soundEnabled`
- Жёстко прописанные пути к файлам (`ClickStandard.mp3`, `Astarion.mp3`)
- Функции `playModeSelect`, `playButtonClick`, `playDefeat` и т.д.

**Проблема:** В engine уже есть полноценная система аудио (`audioPlayer.ts`), которая:
- Поддерживает fallback-и и oscillator
- Работает с manifest-системой загрузки
- Корректно работает с iOS/Safari

**Рекомендация:** Удалить `src/utils/soundEffects.ts` полностью. Весь код, который его использует, давно перешёл на `useAudio` hook.

**Статус:** ✅ Сделано — файл и индекс удалены (Phase 1)

---

### 1.2 Дублирование в `src/engine/utils/assetLoader.ts` vs `src/engine/services/AssetLoader.ts`

Два файла с похожими названиями, но разным назначением:
- `utils/assetLoader.ts` — старые утилиты (`getAssetPaths`, `checkFileExists`, `resolveAssetPath`)
- `services/AssetLoader.ts` — новый singleton сервис с manifest-based загрузкой

**Проблема:** `audioPlayer.ts` импортирует из `utils/assetLoader.ts`, хотя `AssetLoader` сервис уже имеет `resolveUrl()`. Частичное дублирование логики.

**Рекомендация:** Переместить нужные утилиты из `utils/assetLoader.ts` в `services/AssetLoader.ts` или создать единый модуль путей.

**Статус:** 🟡 Частично — BASE_PATH унифицирован; utils/assetLoader остаётся для legacy-fallback (Phase 2)

---

## 2. 📁 СТРУКТУРНЫЕ ПРОБЛЕМЫ

### 2.1 `sandbox/` папка в корне проекта

Содержит:
- `millionaire-sounds.html` — 538-строчный HTML файл для тестирования звуков
- `test-prizes.js` — скрипт тестирования призовой лестницы

**Проблема:** Это development-only файлы, которые:
- Не нужны в production build
- Не связаны с основным кодом
- Захламляют корень проекта

**Рекомендация:** Переместить в `scripts/sandbox/` или добавить в `.gitignore`, либо создать отдельный dev workspace.

**Статус:** ✅ Выполнено — перенесено в `scripts/sandbox/` (Phase 2)

---

### 2.2 `src/utils/` vs `src/engine/utils/`

- `src/utils/` — содержит только `soundEffects.ts` (устаревший) и `index.ts`
- `src/engine/utils/` — актуальные утилиты engine

**Рекомендация:** После удаления `soundEffects.ts` — удалить папку `src/utils/` полностью.

**Статус:** ✅ Выполнено — папка удалена вместе с legacy кодом (Phase 1)

---

### 2.3 Компоненты UI разбросаны

- `src/components/ui/Panel.tsx`, `PanelHeader.tsx` — shared UI
- `src/engine/components/` — engine-specific компоненты

**Проблема:** Engine компоненты (`EndScreen`, `GameScreen`) импортируют из `../../components/ui`, что нарушает модульность engine.

**Рекомендация:** Либо переместить `Panel`, `PanelHeader` в `src/engine/components/ui/`, либо сделать engine полностью автономным.

**Статус:** ✅ Выполнено — Panel/PanelHeader перенесены в `engine/components/ui`, старый barrel реэкспортирует (Phase 4)

---

## 3. 🔧 АРХИТЕКТУРНЫЕ УЛУЧШЕНИЯ

### 3.1 Типизация `EffectsAPI` дублируется

В `GameScreen.tsx` и `EndScreen.tsx` определён одинаковый интерфейс `EffectsAPI`:

```tsx
interface EffectsAPI {
  triggerConfetti: (origin?: { x: number; y: number }) => void;
  triggerCoins: (origin?: { x: number; y: number }) => void;
  // ...
}
```

**Рекомендация:** Вынести в `src/engine/types/index.ts` и экспортировать.

**Статус:** ✅ Выполнено — тип вынесен в `engine/types` и переиспользуется (Phase 1)

---

### 3.2 Default Icons дублируются

В `GameScreen.tsx` и `EndScreen.tsx`:

```tsx
const DefaultCoinIcon = () => <span className="mr-1">🪙</span>;
const DefaultPhoneHintIcon = () => <span>📞</span>;
```

**Рекомендация:** Создать `src/engine/components/DefaultIcons.tsx` и импортировать.

**Статус:** ✅ Выполнено — создан `DefaultIcons.tsx` и подключён (Phase 2)

---

### 3.3 `useAudio` hook слишком большой (~557 строк)

Делает слишком много:
- Управление музыкой
- Управление звуками
- LocalStorage для preferences
- Fallback логика

**Рекомендация:** Разбить на:
- `useMusicPlayer.ts` — музыка
- `useSoundEffects.ts` — звуковые эффекты
- `useAudioPreferences.ts` — localStorage

**Статус:** ⬜ Не начато

---

### 3.4 `audioPlayer.ts` — монолит (~682 строки)

Содержит всё:
- AudioContext управление
- Oscillator fallbacks
- Preload/decode логика
- playSound, playMusic, playVoice

**Рекомендация:** Разбить на модули:
- `audioContext.ts` — создание/управление AudioContext
- `oscillatorSounds.ts` — fallback oscillator звуки
- `audioCache.ts` — кэширование buffers

**Статус:** ⬜ Не начато

---

## 4. ⚠️ ПОТЕНЦИАЛЬНЫЕ ПРОБЛЕМЫ

### 4.1 Хардкод путей к MP3 в устаревшем `soundEffects.ts`

```typescript
const SOUND_FILES = [
  'ClickStandard.mp3',
  'ClickStartAdventure.mp3',
  // ...
];
```

**Проблема:** Engine использует `.ogg`, а старый код — `.mp3`. Рассинхронизация.

**Статус:** ✅ Закрыто — файл удалён (Phase 1)

---

### 4.2 Отсутствие ESLint/Prettier конфигурации

В `package.json` нет:
- `eslint` или `@eslint/js`
- `prettier`
- Husky hooks

**Рекомендация:** Добавить линтинг для поддержания качества кода.

**Статус:** ✅ Выполнено — ESLint + Prettier добавлены (Phase 3–4)

---

### 4.3 Нет тестов

Отсутствуют:
- Unit tests для hooks
- Integration tests для game logic
- Component tests

**Рекомендация:** Добавить Vitest + React Testing Library.

**Статус:** ⬜ Не начато

---

## 5. 📦 ЧЕГО НЕ ХВАТАЕТ

### 5.1 Error Boundaries

Нет обработки ошибок на уровне React. Если `AssetLoader` падает — приложение крашится.

**Рекомендация:** Добавить `ErrorBoundary` компонент.

**Статус:** ✅ Выполнено — ErrorBoundary добавлен и оборачивает App (Phase 3)

---

### 5.2 Централизованные константы

Магические числа разбросаны:
- `MAX_QUESTIONS_PER_TIER = 5`
- `transitionDuration: 1500`
- `displayDuration: 4000`

**Рекомендация:** Создать `src/engine/constants.ts`.

**Статус:** ✅ Выполнено — создан `engine/constants.ts`, применён в вопросах/аудио (Phase 3)

---

### 5.3 Логирование

Используется `console.log`, `console.warn` напрямую.

**Рекомендация:** Создать простой logger-сервис с уровнями и возможностью отключения в production.

**Статус:** ✅ Частично — добавлен `logger` сервис, применён в AssetLoader/ErrorBoundary/MillionaireGame (Phase 4); остальные места используют console.*

---

### 5.4 Documentation

- Нет JSDoc для публичных API
- README.md возможно устарел (упоминает структуру, которая изменилась)

**Статус:** ⬜ Не начато

---

## 6. 📋 СВОДНАЯ ТАБЛИЦА ДЕЙСТВИЙ

| Приоритет | Действие | Файлы | Статус |
|-----------|----------|-------|--------|
| 🔴 Высокий | Удалить устаревший код | `src/utils/soundEffects.ts`, `src/utils/index.ts` | ✅ |
| 🔴 Высокий | Убрать дубликаты типов | `EffectsAPI` в 2 файлах | ✅ |
| 🟡 Средний | Консолидировать assetLoader | `utils/assetLoader.ts` → `services/` | 🟡 Частично |
| 🟡 Средний | Переместить sandbox | `sandbox/` → `scripts/sandbox/` | ✅ |
| 🟡 Средний | Вынести DefaultIcons | Из `GameScreen`, `EndScreen` | ✅ |
| 🟢 Низкий | Разбить большие файлы | `useAudio.ts`, `audioPlayer.ts` | ⬜ |
| 🟢 Низкий | Добавить ESLint | `package.json` | ✅ |
| 🟢 Низкий | Добавить ErrorBoundary | Новый компонент | ✅ |
| 🟢 Низкий | Добавить тесты | Vitest setup | ⬜ |
| 🟢 Низкий | Централизовать константы | `src/engine/constants.ts` | ✅ |
| 🟢 Низкий | Добавить logger | `src/engine/services/logger.ts` | ✅ (частично внедрён) |

---

## 7. 📝 ИСТОРИЯ ИЗМЕНЕНИЙ

| Дата | Изменение | Автор |
|------|-----------|-------|
| 2025-12-07 | Создан документ анализа | — |
| 2025-12-07 | Обновлены статусы после фаз 1–4 | codex |

---

*Этот документ обновляется по мере выполнения рефакторинга.*
